{% extends 'base.html.twig' %}

{% block title %}EtudiantMatiere{% endblock %}

{% block body %}
    <h1>EtudiantMatiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etudiant_matiere.id }}</td>
            </tr>
            <tr>
                <th>Absences</th>
                <td>{{ etudiant_matiere.absences }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_math_index') }}">back to list</a>

    <a href="{{ path('app_math_edit', {'id': etudiant_matiere.id}) }}">edit</a>

    {{ include('math/_delete_form.html.twig') }}
{% endblock %}
