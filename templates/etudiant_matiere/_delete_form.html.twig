<form method="post" action="{{ path('app_etudiant_matiere_delete', {'id': etudiant_matiere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etudiant_matiere.id) }}">
    <button class="btn">Delete</button>
</form>
